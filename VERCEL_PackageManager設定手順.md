# 📦 Vercel Package Manager設定手順

## 重要なポイント

**Vercelには「Package Manager」という直接的な設定項目はありません。**

代わりに、Vercelはプロジェクト内のロックファイルを自動検出して、使用するパッケージマネージャーを決定します：

- `package-lock.json` が存在 → npm を使用
- `pnpm-lock.yaml` が存在 → pnpm を使用
- `yarn.lock` が存在 → yarn を使用

## 🔍 現在の問題

ビルドログを見ると：
```
Detected `pnpm-lock.yaml` 9 which may be generated by pnpm@9.x or pnpm@10.x
```

これは、`pnpm-lock.yaml` が存在するため、Vercelがpnpmを使おうとしていることを示しています。

## ✅ 解決方法

### 方法1: `pnpm-lock.yaml` を削除（推奨）

`pnpm-lock.yaml` を削除することで、Vercelは自動的にnpmを使用します。

#### Step 1: ローカルで `pnpm-lock.yaml` を削除

```bash
cd blog-writer-app
rm pnpm-lock.yaml
```

#### Step 2: `.gitignore` に追加（既に追加済み）

`.gitignore` に以下が追加されていることを確認：
```
pnpm-lock.yaml
yarn.lock
```

#### Step 3: `package-lock.json` を確実に存在させる

`package-lock.json` が存在することを確認：

```bash
cd blog-writer-app
npm install
```

これにより、`package-lock.json` が生成または更新されます。

#### Step 4: GitHubにプッシュ

```bash
git add .gitignore package-lock.json
git rm pnpm-lock.yaml  # まだGitHubに存在する場合
git commit -m "Remove pnpm-lock.yaml and use npm"
git push
```

### 方法2: Vercelダッシュボードで確認

1. **Vercelダッシュボードにアクセス**
   - https://vercel.com にログイン
   - プロジェクト「blog-generator」を選択

2. **Settings > Build and Deployment を開く**
   - 左サイドバーから「Settings」をクリック
   - 「Build and Deployment」タブをクリック

3. **「Production Overrides」セクションを確認**
   - 「Production Overrides」セクションを展開
   - パッケージマネージャーに関する上書き設定がないか確認

4. **「Project Settings」セクションを確認**
   - 「Project Settings」セクションを展開
   - フレームワーク設定を確認

## 🔍 確認方法

### ビルドログで確認

再デプロイ後、ビルドログで以下を確認：

**正しい場合（npmを使用）:**
```
Detected `package-lock.json`
Using npm
Running "install" command: `npm install --legacy-peer-deps`...
```

**間違っている場合（pnpmを使用）:**
```
Detected `pnpm-lock.yaml`
Using pnpm@10.x
```

## 📝 推奨される手順

### Step 1: ローカルで `pnpm-lock.yaml` を削除

```bash
cd blog-writer-app
rm pnpm-lock.yaml
```

### Step 2: `package-lock.json` を生成

```bash
npm install
```

これにより、`package-lock.json` が生成または更新されます。

### Step 3: 変更をGitHubにプッシュ

```bash
git add package-lock.json
git rm pnpm-lock.yaml  # まだGitHubに存在する場合
git commit -m "Remove pnpm-lock.yaml and use npm"
git push
```

### Step 4: Vercelで再デプロイ

1. **Vercelダッシュボードで再デプロイ**
   - 「Deployments」タブを開く
   - 最新のデプロイメントの右端の「⋯」をクリック
   - 「Redeploy」を選択

2. **ビルドログを確認**
   - ビルドログで `Detected package-lock.json` と表示されることを確認
   - `npm install --legacy-peer-deps` が実行されることを確認

## ⚠️ 重要な注意事項

1. **`pnpm-lock.yaml` を削除する**
   - このファイルが存在すると、Vercelはpnpmを使おうとします
   - `.gitignore` に追加済みですが、既にGitHubに存在する場合は削除が必要です

2. **`package-lock.json` を確実に存在させる**
   - `npm install` を実行して、`package-lock.json` を生成または更新してください

3. **Vercelの設定を確認**
   - 「Install Command」が `npm install --legacy-peer-deps` に設定されていることを確認
   - 「Root Directory」が `blog-writer-app` に設定されていることを確認

## 🚀 次のステップ

1. **ローカルで `pnpm-lock.yaml` を削除**
2. **`npm install` を実行して `package-lock.json` を生成**
3. **変更をGitHubにプッシュ**
4. **Vercelで再デプロイ**
5. **ビルドログで確認**

---

**作成日**: 2025年1月27日  
**バージョン**: 1.0

